#!/usr/bin/env python3

import sys

import _utils

parser = _utils.create_parser()
parser.parse_args()

pr_url = _utils.check_output(
    ["gh", "pr", "view", "--json", "url", "--jq", ".url"]
).strip()

_utils.copy_to_clipboard(pr_url)
print("\nThe below has been copied to your clipboard! 📋\n", file=sys.stderr)
print(pr_url)
