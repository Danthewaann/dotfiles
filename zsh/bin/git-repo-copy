#!/usr/bin/env python3

import sys

import _utils

repo_url = _utils.check_output(
    ["gh", "repo", "view", "--json", "url", "--jq", ".url"]
).strip()

_utils.copy_to_clipboard(repo_url)
print("\nThe below has been copied to your clipboard! 📋\n", file=sys.stderr)
print(repo_url)
