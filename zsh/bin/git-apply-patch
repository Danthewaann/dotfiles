#!/usr/bin/env python3

import platform
import subprocess
import _utils

if platform.system() == "Darwin":
    command = ["pbpaste"]
else:
    command = ["xclip", "-selection", "clipboard", "-o"]

patch = _utils.check_output(command)

subprocess.run(["git", "apply", "-"], text=True, input=patch)
