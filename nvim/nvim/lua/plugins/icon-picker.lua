return {
  "ziontee113/icon-picker.nvim",
  keys = {
    { "<C-p>", "<cmd>IconPickerInsert emoji<CR>", desc = "Pick an emoji to insert", mode = "i" }
  },
  opts = { disable_legacy_commands = true }
}
